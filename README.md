# Gulp sass+browser-sync
### Dependences
* Installed NPM
* Global installed Gulp-cli ( npm i -g gulp-cli )
### Install dev dependencies
`npm install`
### Start dev server
`npm run serve` 
or 
`gulp`
### Build project
`npm run build` 
or 
`gulp build`

